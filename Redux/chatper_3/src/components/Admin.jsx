function Admin() {
  return (
    <div className="card">
      <div className="container">
        <h4>
          <b>Admin Component</b>
        </h4>
        <h3>Total Point : ${}</h3>

        {/* <button onClick={() => dispatch(increment())}>Increment +</button> */}
      </div>
    </div>
  );
}

export default Admin;
